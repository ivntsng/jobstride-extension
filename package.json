{
  "name": "nextstep-extension",
  "version": "1.0.0",
  "description": "A Chrome extension to track job applications",
  "scripts": {
    "build": "node build.js",
    "build:watch": "tsc --watch",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prebuild": "pnpm run clean",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
